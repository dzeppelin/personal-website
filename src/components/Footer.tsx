import React from 'react';
import { FC } from 'react';

const Footer: FC = () => {
    return (
        <footer>
            <p>footer</p>
        </footer>
    );
};

export default Footer;
